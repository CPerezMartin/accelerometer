import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bfi.accelerometer.app',
  appName: 'accelerometer',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
